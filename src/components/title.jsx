import React from "react";

const Title = props => (
  <div className="jumbotron">
    <h1 className="display-3">Weather Finder</h1>
    <p className="lead">
      Use this simple ReactJS App to search the current weather conditions in
      major cities around the world.
    </p>
  </div>
);

export default Title;
